services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_BASE_API=${VITE_BASE_API:-http://localhost:3000}
    stdin_open: true
    tty: true
